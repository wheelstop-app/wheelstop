<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Legacy `Theme.CamExploit` and `Theme.BydChamp` aliases were removed
      in the M3 cleanup pass. The active dark theme lives in
      values-night/themes_wheelstop.xml as Theme.Wheelstop.M3 — the
      manifest entry (`@style/Theme.Wheelstop.M3`) auto-flips with night
      mode.
    -->
</resources>
