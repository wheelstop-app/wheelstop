<?xml version="1.0" encoding="utf-8"?>
<!--
  Place-action bottom sheet — shown when the user long-presses the map (a
  dropped pin) or long-presses a search-result row. It makes saving a favourite
  a one-tap action from anywhere, instead of having to compute a route first.
  Header = pin icon + place name + subtitle; actions = "Navigate here" plus a
  three-way Save row (Home / Work / Favourite). All theme attrs so it is correct
  in day + night.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="12dp"
    android:paddingBottom="24dp">

    <View
        android:layout_width="32dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="@drawable/sheet_drag_handle" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/ivPlaceIcon"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/bg_nav_leading_icon"
            android:contentDescription="@null"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_location_pin"
            app:tint="?attr/colorPrimary" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvPlaceName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
                android:textColor="?attr/colorOnSurface"
                tools:text="Marina Bay Sands" />

            <TextView
                android:id="@+id/tvPlaceSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
                android:textColor="?attr/colorOnSurfaceVariant"
                tools:text="Bayfront Avenue, Singapore" />
        </LinearLayout>
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnPlaceNavigate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:paddingVertical="14dp"
        android:text="@string/roadsense_map_navigate_here"
        app:icon="@drawable/ic_directions"
        app:iconGravity="textStart" />

    <!-- One-tap save row: Home / Work / Favourite. Tonal buttons with equal
         weight so saving is reachable without computing a route first. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal"
        android:baselineAligned="false">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPlaceSaveHome"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="5dp"
            android:paddingVertical="14dp"
            android:text="@string/roadsense_map_saved_home"
            app:icon="@drawable/ic_saved_home"
            app:iconGravity="textStart" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPlaceSaveWork"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginHorizontal="5dp"
            android:paddingVertical="14dp"
            android:text="@string/roadsense_map_saved_work"
            app:icon="@drawable/ic_saved_work"
            app:iconGravity="textStart" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPlaceSaveFav"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="5dp"
            android:paddingVertical="14dp"
            android:text="@string/roadsense_map_saved_favourite"
            app:icon="@drawable/ic_star"
            app:iconGravity="textStart" />
    </LinearLayout>

    <!-- Hand the place to the car's built-in navigation (factory nav) -->
    <TextView
        android:id="@+id/carNavSectionHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:layout_marginBottom="6dp"
        android:text="@string/carnav_section_header"
        android:textAppearance="?attr/textAppearanceLabelMedium"
        android:alpha="0.7" />

    <LinearLayout
        android:id="@+id/carNavActionsRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:baselineAligned="false">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCarNavNavigate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="5dp"
            android:paddingVertical="14dp"
            android:text="@string/carnav_navigate" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCarNavSave"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="5dp"
            android:paddingVertical="14dp"
            android:text="@string/carnav_save_favourite" />
    </LinearLayout>

</LinearLayout>
