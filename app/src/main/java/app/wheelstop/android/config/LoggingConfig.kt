package app.wheelstop.android.config

import app.wheelstop.android.logging.LogConfig

/**
 * Alias for LogConfig to keep it in config package.
 * This allows ConfigManager to manage all configs in one place.
 */
typealias LoggingConfig = LogConfig
